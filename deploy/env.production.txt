# ============================================================
# GOIL Budget System — Production Environment Template
# Rename this file to .env and fill in every value marked <<<
# DO NOT commit this file to any git repository
# ============================================================

APP_NAME="GOIL Budget System"
APP_ENV=production
APP_KEY=                         # Generated automatically by install.ps1
APP_DEBUG=false
APP_URL=https://budget.goil.com  # <<< Change to actual server URL

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US
APP_MAINTENANCE_DRIVER=file

BCRYPT_ROUNDS=12

# ── Logging ──────────────────────────────────────────────────
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error                  # error in production (not debug)

# ── Database ─────────────────────────────────────────────────
DB_CONNECTION=mysql
DB_HOST=127.0.0.1                # <<< Change if MySQL is on a separate server
DB_PORT=3306
DB_DATABASE=goil_budget
DB_USERNAME=goilbudget           # <<< Use a dedicated DB user, NOT root
DB_PASSWORD=                     # <<< Set a strong password

# ── Sessions ─────────────────────────────────────────────────
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=true             # Must be true in production
SESSION_PATH=/
SESSION_DOMAIN=.goil.com         # <<< Your domain (with leading dot covers subdomains)

# ── Cache & Queue ────────────────────────────────────────────
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
CACHE_STORE=database

# ── Mail ─────────────────────────────────────────────────────
# Configure this or users will NOT receive password resets or approval emails
MAIL_MAILER=smtp
MAIL_HOST=mail.goil.com          # <<< GOIL mail server
MAIL_PORT=587
MAIL_USERNAME=budget-system@goil.com   # <<< Service email account
MAIL_PASSWORD=                         # <<< Mail account password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=budget-system@goil.com
MAIL_FROM_NAME="GOIL Budget System"

# ── LDAP / Active Directory (if SSO is used) ─────────────────
# Leave blank if SSO is disabled in System Settings
LDAP_CONNECTION=default
LDAP_HOST=                       # <<< e.g. ad.goil.com
LDAP_PORT=389
LDAP_BASE_DN=                    # <<< e.g. dc=goil,dc=com
LDAP_USERNAME=                   # <<< Service account: svc_budget@goil.com
LDAP_PASSWORD=                   # <<< Service account password
LDAP_TIMEOUT=5
LDAP_SSL=false
LDAP_TLS=false
LDAP_SCHEMA=ActiveDirectory
LDAP_USER_ATTRIBUTE=samaccountname
LDAP_USER_EMAIL_ATTRIBUTE=mail
LDAP_USER_NAME_ATTRIBUTE=cn
LDAP_USER_DEPARTMENT_ATTRIBUTE=department
LDAP_USER_EMPLOYEE_ID_ATTRIBUTE=employeenumber
LDAP_ACCOUNT_SUFFIX=@goil.com

# ── Misc ─────────────────────────────────────────────────────
VITE_APP_NAME="${APP_NAME}"
